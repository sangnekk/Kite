---
sidebar_position: 29
---

import EmbedFlowNode from "../../../../src/components/EmbedFlowNode";
import NodeInfoExplorer from "../../../../src/components/NodeInfoExplorer";

# Lấy biến lưu trữ

<EmbedFlowNode type="action_variable_get" />

Khối `Get stored variable` được dùng để lấy giá trị của biến lưu trữ. Giá trị này có thể được dùng ở các khối phía sau qua biến `result(...)`.

<NodeInfoExplorer type="action_variable_get" />
